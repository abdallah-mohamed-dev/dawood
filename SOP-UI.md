# SOP-UI — تصميم وتجربة المستخدم (Dawood)

مرجع واحد لحالة الـ UI/UX الحالية في النظام، عشان يُستخدم كنقطة انطلاق لإعادة تصميم الواجهة.

---

## 1. نظرة عامة على الوضع الحالي

الواجهة كلها **Blade + Tailwind CSS v4** (لا Livewire ولا Vue/React) — يعني كل تفاعل تقريبًا بيعمل **Full Page Reload** (submit فورم → تحديث الصفحة كلها). مفيش أي حالات تحميل (loading states)، ولا تفاعل لحظي (AJAX)، ولا أي Animation حقيقي غير الـ hover transitions البسيطة.

- **اللغة:** عربي بالكامل، RTL (`dir="rtl"`).
- **الخط:** Cairo.
- **الشكل العام:** تصميم "ورشة" متعمّد — بني نحاسي/خشبي دافئ بدل الأزرق التقني المعتاد (موثق في `resources/css/app.css`).
- **لا يوجد** نظام Dark Mode، ولا صفحة إعدادات مستخدم، ولا صورة بروفايل — بس زرار "تسجيل خروج".

## 2. نظام التصميم الحالي (Design Tokens)

من `resources/css/app.css`:

| العنصر | القيمة |
|---|---|
| اللون الأساسي (Primary) | `#b8672e` (نحاسي) |
| لون الثانوي (Secondary/نص خافت) | `#6b5f52` |
| النجاح | `#5b7a45` (أخضر زيتي) |
| الخطر | `#b8452f` (أحمر طوبي) |
| التحذير | `#c08a2e` (أصفر نحاسي) |
| الخلفية | `#f6f1e7` (كتان فاتح) |
| السطح (كروت/جداول) | `#fffdf8` |
| الحدود | `#e7dcc9` |
| الشريط الجانبي (Sidebar) | `#2e2013` (بني خشب غامق) — العنصر البصري المميز الوحيد فعليًا |

**المكونات المشتركة الموجودة:** `data-table` (جدول عام)، `field` (حقل نموذج)، `delete-button` (زر حذف مع تأكيد)، `money` / `quantity` (عرض قيم)، `status-badge`، `app-layout` / `guest-layout`.

كل الكروت في النظام بنفس الشكل تمامًا: `rounded-xl border border-border bg-surface p-4 shadow-sm` — مفيش تدرج بصري (Visual Hierarchy) بين كارت مهم وكارت عادي.

## 3. الهيكل العام (Layout)

- **Sidebar** ثابت (64 وحدة عرض) على الشاشات الكبيرة، بيتحول لـ Drawer منزلق على الموبايل (checkbox toggle، بدون JS framework).
- **Sidebar فيه 10 عناصر نص فقط، بدون أيقونات، بدون تجميع منطقي**: لوحة التحكم، العملاء، التصنيفات، المواد، المشتريات، المدفوعات، المصروفات، الخزنة، الشركاء، تقرير الربح — كلهم في مستوى واحد متتالي.
- **المحتوى الرئيسي:** حاوية واحدة `max-w-6xl` في النص، بلا Breadcrumbs، بلا عنوان صفحة موحّد التنسيق (كل صفحة بتكرر `<h1>` بنفسها).
- **رسائل النجاح/الخطأ:** Banner واحد أعلى الصفحة (أخضر/أحمر) بعد أي عملية.

## 4. وصف الصفحات الحالية (إيه اللي المستخدم بيشوفه فعليًا)

### تسجيل الدخول
فورم بسيط في المنتصف (بدون Sidebar): إيميل، باسورد، "تذكرني"، زرار دخول. تصميم نظيف لكن جاف تمامًا — مفيش شعار حقيقي أو صورة أو أي هوية بصرية غير النص.

### لوحة التحكم (Dashboard)
ترحيب باسم المستخدم + شبكة من 6 كروت "اختصارات" (كل كارت = اسم قسم + جملة وصف قصيرة + سهم). **مفيش أي أرقام أو مؤشرات حقيقية هنا** — لا رصيد الخزنة، لا صافي الربح، لا عدد الغرف الحالية. الداشبورد فعليًا قائمة روابط مموّهة بشكل كروت، مش لوحة معلومات.

### الخزنة (Cashbox)
3 كروت أرقام (الرصيد، إجمالي داخل، إجمالي خارج) + فورم الرصيد الافتتاحي + جدول كل الحركات (تاريخ، نوع، بند، وصف، مبلغ) مع Pagination افتراضي من Laravel. لا فلترة بالتاريخ أو النوع، لا رسم بياني.

### التصنيفات / المواد / المشتريات (المخزون)
صفحات CRUD قياسية: جدول + زرار "إضافة" أعلى يمين، وصفحات create/edit منفصلة بفورم بسيط (حقول `field` تحت بعض عموديًا). صفحة المواد بتعرض "الكمية الحالية" كعمود في الجدول.

### العملاء
جدول عملاء (اسم، تليفون، عنوان) + صفحة عرض تفصيلية لكل عميل تجمع غرفه. الفورم زي باقي الشاشات (create/edit منفصلين).

### الغرف (صفحة `rooms.show`) — **أكثر صفحة كثافة في النظام**
صفحة واحدة طويلة فيها كل حاجة سوا:
- 4 كروت أرقام (سعر البيع، تكلفة الخامات، المدفوع، المتبقي).
- Dropdown لتغيير الحالة **بيعمل submit تلقائي أول ما تختار (`onchange="this.form.submit()"`) — بدون أي تأكيد** — تغيير خطير (زي تحويل الغرفة لـ"مكتملة") ممكن يحصل بالغلط بضغطة واحدة.
- فورم "إضافة احتياج مادة" inline.
- جدول مواد الغرفة، وفيه فورم "صرف" مصغّر جوه كل صف من الجدول (input + زرار) — كثافة عالية داخل الجدول نفسه.
- فورم "إضافة دفعة" inline.
- جدول الدفعات.
- Modal (`<dialog>`) لحذف الغرفة لو اتصرفلها خامات، بيسأل "ترجيع ولا استهلاك" عبر Radio buttons.

الصفحة دي محتاجة إعادة تنظيم بصري، مش مجرد تلوين — المحتوى كتير جدًا في عمود واحد بدون Tabs أو أقسام قابلة للطي.

### المدفوعات / المصروفات
جداول قياسية + create/edit، نفس نمط باقي الشاشات.

### الشركاء
جدول (اسم، نسبة، نصيب، سحوبات، متبقي) + Banner تحذيري أصفر لو الربح ≤ صفر، وBanner أحمر لو فيه شريك سحب أكتر من نصيبه. صفحة `partners.show` فيها تفاصيل السحوبات لشريك واحد.

### تقرير الربح
أهم صفحة ماليًا بصريًا: Banner توضيحي أعلى الصفحة يشرح الفرق بين "رصيد الخزنة" و"صافي الربح"، بعدين كارتين كبار (الخزنة / الربح) بلون مشروط (أخضر لو موجب، أحمر لو سالب)، وتحتهم 5 كروت أصغر (الإيراد، تكلفة الخامات، المصروفات، WIP، قيمة المخزون). **مفيش أي Chart** — كله أرقام في كروت، ومفيش تاريخ/فترة زمنية يقدر المستخدم يفلتر بيها التقرير (كل الأرقام "حتى الآن" فقط).

## 5. تقييم عام لنقاط الضعف الحالية

1. **الداشبورد فاضي من المعلومات** — أهم شاشة في أي نظام مالي مفروض توري ملخص سريع (رصيد، ربح، تنبيهات)، وهنا هي مجرد قائمة روابط.
2. **Sidebar بلا تجميع ولا أيقونات** — 10 عناصر متتالية بدون تصنيف (مالي / مخزون / عملاء) بيصعّب المسح البصري السريع، خصوصًا مع نمو النظام.
3. **صفحة الغرفة مثقلة** — كل شيء في عمود واحد طويل بلا Tabs/Accordion، صعب المتابعة على شاشة صغيرة أو لعميل بيه غرف كتير عمليات.
4. **تغيير حالة الغرفة بلا تأكيد** — إجراء حرج (زي "مكتملة") بيتنفذ بمجرد اختيار من Dropdown، فيه احتمال خطأ بشري عالي.
5. **لا يوجد Feedback فوري** — كل عملية = Reload كامل للصفحة، مفيش Toast أو تحديث جزئي، فالتجربة بتحس بطيئة وقديمة.
6. **لا تصور بياني (Charts) في أي مكان**، رغم إن التقرير المالي وصفحة الخزنة أماكن مثالية لرسم بياني بسيط (اتجاه الرصيد، الربح عبر الوقت).
7. **الجداول أساسية جدًا** — لا بحث، لا فرز (Sort)، لا فلترة، لا تصدير — مصدر إزعاج حقيقي لما عدد الصفوف يكبر (عملاء/حركات خزنة كتير مثلًا).
8. **تناسق بصري صحيح لكن رتيب** — كل الكروت بنفس الشكل بالظبط بدون أي تدرج بصري (الأرقام الأهم زي "صافي الربح" لازم تبان أكبر/أوضح من رقم عادي).
9. **لا حالات فارغة أو تحميل مصممة** — رسالة "لا نتائج" نصية بسيطة فقط، ومفيش Skeleton/Spinner أثناء أي عملية.
10. **الهوية البصرية محدودة** — شعار الشركة مجرد حرف "D" في مربع، مفيش أي Illustration أو عنصر بصري يعكس طبيعة "ورشة أثاث" فعليًا رغم إن لوحة الألوان مستوحاة من هذا المفهوم.

## 6. اقتراحات للتصميم الجديد

- **داشبورد حقيقي:** رصيد الخزنة + صافي الربح + عدد الغرف حسب الحالة + آخر الحركات + تنبيهات (سحب شريك زايد، غرف متأخرة) في نظرة واحدة.
- **Sidebar مقسّم لمجموعات:** "الإدارة المالية" (خزنة، مدفوعات، مصروفات، شركاء، تقرير الربح) / "المخزون" (تصنيفات، مواد، مشتريات) / "العملاء والغرف" — مع أيقونة بسيطة لكل عنصر.
- **صفحة الغرفة كـ Tabs:** تبويب "التفاصيل المالية"، تبويب "المواد"، تبويب "الدفعات" — بدل عمود واحد طويل.
- **تأكيد صريح لتغيير الحالة** (خصوصًا "مكتملة")، بدل Auto-submit من Dropdown.
- **رسم بياني بسيط** لاتجاه رصيد الخزنة وصافي الربح عبر الوقت في صفحة التقرير + فلتر بفترة زمنية.
- **تدرج بصري للأرقام المهمة:** صافي الربح ورصيد الخزنة يستحقوا حجم خط وبروز أكبر من أي رقم فرعي.
- **بحث وفرز أساسي في الجداول** (خصوصًا العملاء، حركات الخزنة، المدفوعات).
- **مؤشرات لحظية (Toast) بدل الاعتماد الكلي على Reload**، حتى لو الفريم وورك ثابت Blade — ممكن تحسين تدريجي بـ Alpine.js خفيف بدل قفزة كاملة لـ SPA.
- **هوية بصرية أقوى:** شعار حقيقي، وربط اللوحة الدافئة (نحاس/خشب) بعناصر بصرية فعلية (Icons مناسبة لصناعة الأثاث) مش لون خلفية بس.

---

*مبني على قراءة فعلية لملفات `resources/views/**`، `resources/css/app.css`، ومكونات `resources/views/components/` وقت كتابة هذا الملف (فرع `main`، بعد commit `9bdae82`).*
