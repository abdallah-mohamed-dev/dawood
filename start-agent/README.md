# ابدأ هنا — نقطة الدخول لأي Agent/موديل جديد

هذا المجلد مكتوب ليكون **قابلاً للقراءة من أي نموذج ذكاء اصطناعي** (Claude، GPT، Gemini، أو أي موديل تاني)، مش بس Claude. لو انت Agent جديد اتفتحت على المشروع دا لأول مرة (سواء بدل موديل قديم أو جلسة جديدة تمامًا)، اتبع الترتيب دا **بالحرف** قبل ما تكتب أي سطر كود.

## ⚠️ القاعدة الأولى: ممنوع تبدأ كود قبل ما تقرأ كل حاجة هنا

لو خالفت الترتيب دا هتكرر أخطاء اتصلحت قبل كدا، أو هتنفذ تاسك بترتيب غلط، أو هتكسر قرار معماري متفق عليه بالفعل. القراءة دي مش اختيارية.

## ترتيب القراءة الإلزامي

1. **الملف دا (`README.md`)** — كامل، قبل أي حاجة تانية.
2. **[`PROJECT.md`](PROJECT.md)** — إيه هو المشروع، التقنيات، القرارات المعمارية الحرجة، وخريطة الملفات.
3. **[`PITFALLS.md`](PITFALLS.md)** — كل الأخطاء اللي حصلت فعلاً في الجلسات اللي فاتت وسبب حصولها وطريقة تجنبها. **لازم تتقرأ كاملة** — دي مش نظرية، دي أخطاء حقيقية اتصلحت بصعوبة.
4. **[`AGENT_LOG.md`](AGENT_LOG.md)** — سجل كل جلسة عمل سابقة. اقرأ آخر 3-4 إدخالات على الأقل عشان تفهم آخر حاجة اتعملت وآخر قرار اتاخد.
5. **[`../docs/tasks.md`](../docs/tasks.md)** — الخطة الأصلية الكاملة والمعتمدة (القسم أ = قرارات مغلقة لا تُناقَش، القسم ب = قواعد تنفيذ إلزامية، القسم ج = تفاصيل كل Task من 0 لـ11).
6. **[`../docs/tasks-checklist.md`](../docs/tasks-checklist.md)** — **هذا هو المرجع الوحيد لمعرفة "احنا واقفين فين"**. كل تاسك فرعي معلّم ✔ لو خلص. اللي مش معلّم = لسه ما اتعملش.
7. **[`../AGENTS.md`](../AGENTS.md)** — قواعد Laravel/Pint/Pest التقنية (مولّدة من Laravel Boost — اتبعها دايمًا).
8. باقي ملفات `../docs/*.md` — كل ملف فيهم بيوثّق منطق عمل جزء معين (المخزون، المدفوعات، الربح...). ارجع لها **وقت الحاجة فقط** لو التاسك اللي شغال عليه يخصها — مش لازم تقراها كلها الأول.

## دورة العمل الإلزامية لأي تاسك (بدون استثناء)

```text
1. اقرأ التاسك في docs/tasks.md (التفاصيل والـAcceptance Criteria)
2. تأكد إنه فعلاً مش متعلّم ✔ في docs/tasks-checklist.md
3. نفّذ (migrations → models → services → requests → controllers → routes → views)
4. php artisan test --compact   ← لازم كل الاختبارات تنجح (القديمة والجديدة)
5. vendor/bin/pint --dirty --format agent
6. لو التاسك فيه واجهة: npm run build (أو تأكد إن الأصول محدَّثة)
7. علّم ✔ على كل بند خلص في docs/tasks-checklist.md (بند بند، مش التاسك كله دفعة واحدة)
8. أضف إدخال جديد في AGENT_LOG.md (الصيغة موجودة في نفس الملف)
```

**ممنوع الانتقال لتاسك تاني قبل ما التاسك الحالي يخلّص الخطوات التمنية دي كاملة.**

## قواعد لا تتفاوض عليها (ملخّص — التفاصيل في PROJECT.md وPITFALLS.md)

- **الأرقام المالية والكميات دايمًا أعداد صحيحة مقيّسة** (المبالغ ×100 قروش، الكميات ×1000) — **ممنوع نهائيًا استخدام `float`** في أي منطق أعمال. استخدم `MoneyCast`/`QuantityCast` الموجودين.
- **كل حركة خزنة لازم تعدي من `CashboxService`** — ممنوع إنشاء `CashboxTransaction` مباشرة من أي مكان تاني.
- **كل عملية بتلمس أكتر من جدول = `DB::transaction()`** واحدة، مع `lockForUpdate()` وإعادة جلب النسخة الطازة من قاعدة البيانات (مش الاعتماد على النسخة اللي جاية من الكولر) — راجع `PITFALLS.md` بند "TOCTOU".
- **ممنوع استخدام tinker أو verification scripts للتحقق** — الدليل الوحيد المقبول هو اختبارات Pest.
- **ممنوع تعديل ملفات خارج نطاق التاسك** إلا لو فيه ضرورة واضحة تتكتب صراحة في AGENT_LOG.md.
- **كل تاسك جديد يحتاج Factory + اختبارات Pest** (المسار الطبيعي + حالات الخطأ + رقم مرجعي محسوب يدويًا).
- **الألوان والخطوط تُعرَّف حصرًا في `resources/css/app.css`** عبر `@theme` — ممنوع أي hex مباشر في أي Blade file.
- **لو التاسك المطلوب "تعديل شكل/واجهة فقط"** (زي Task 8.6/8.7 في السجل) — **ممنوع لمس أي منطق PHP في `app/`**، الشغل كله في `resources/views/` و`resources/css/`.

## الحالة الحالية بسرعة (تفاصيل كاملة في `docs/tasks-checklist.md`)

- Tasks 0 → 8.7: **مكتملة بالكامل** (Backend + Frontend + اختبارات).
- Task 9 (الشركاء والسحوبات): **لم يبدأ**.
- Task 10 (تجانس الواجهة الكامل): بدأ جزئيًا فقط.
- Task 11 (اختبار الدورة الكاملة): **لم يبدأ**.
- آخر commit في git: `de318f1` ("part 3") — يحتوي كل شغل Tasks 0-8. تعديلات الـstyle بتاعة Task 8.6/8.7 **لسه غير مُرحّلة (uncommitted)** وقت كتابة هذا الملف — تأكد من `git status` قبل ما تبدأ.

## بيانات تشغيل سريعة

```bash
composer install && npm install
cp .env.example .env   # لو مش موجود بالفعل
php artisan key:generate
php artisan migrate:fresh --seed
npm run build           # أو npm run dev / composer run dev أثناء التطوير
php artisan serve       # أو استخدم Herd لو مفعّل على localhost:8000
```

- تسجيل الدخول (بيئة تطوير محلية فقط): `admin@dawood.test` / `password` (من `.env.example`).
- التطبيق عربي RTL بالكامل — لا تكتب أي نص واجهة بالإنجليزي إلا لو موجود بالفعل في الكود.
