# سجل عمل الـAgents — Append-Only

هذا الملف **بيتكتب فيه فقط ولا يُعدَّل فيه** (append-only). كل Agent/موديل بيشتغل على المشروع لازم:

1. **يضيف إدخال "بداية جلسة"** فور ما يبدأ الشغل (حتى قبل أي كود) — عشان لو حصل انقطاع، الإدخال يوضح إنه كان شغال وعلى إيه.
2. **يضيف إدخال "نهاية جلسة/تاسك"** فور ما يخلّص أي تاسك أو جزء منه — حتى لو التاسك مش خلصان بالكامل.
3. **ميحذفش ولا يعدّل** أي إدخال قديم — بس يضيف إدخال جديد في آخر الملف.

## الصيغة (انسخها والصقها في آخر الملف، واملأها)

```markdown
---
### [التاريخ YYYY-MM-DD] — [اسم الموديل/الـAgent لو معروف، وإلا "غير محدد"]

**النوع:** بداية جلسة / نهاية تاسك / تحديث جزئي

**التاسك:** [رقم ووصف التاسك من docs/tasks.md — مثال: "Task 9 — الشركاء والسحوبات"]

**اللي اتعمل:**
- [نقطة 1]
- [نقطة 2]

**حالة الاختبارات:** [عدد الاختبارات قبل] → [عدد الاختبارات بعد] (كلها ناجحة / فيه فشل: اذكره)

**بنود docs/tasks-checklist.md اتعلّمت:** [اذكر البنود أو "لا يوجد بعد"]

**ملاحظات لأي Agent جاي:** [أي حاجة محتاج الشخص اللي بعدك ياخد باله منها — قرار اتاخد، مشكلة واجهتك، حاجة لسه ناقصة]
---
```

## الإدخالات

---
### 2026-08-20 — Claude (Sonnet 5, عبر Claude Code / VS Code extension)

**النوع:** إدخال أساسي (baseline) — تأسيس هذا السجل بأثر رجعي بعد إنهاء Tasks 0–8.7

**التاسك:** Tasks 0 → 8.7 بالكامل (تراكمي عبر عدة جلسات سابقة قبل إنشاء هذا الملف)

**اللي اتعمل (ملخّص):**
- Task 0: الأساس (Auth يدوي، RTL، Design Tokens، MoneyCast/QuantityCast، إعدادات SQLite للتزامن)
- Task 1: كل ملفات `docs/*.md` التوثيقية
- Task 2: `CashboxService` وصفحة `/cashbox`
- Task 3: التصنيفات والمواد (`Inventory\CategoryController`, `Inventory\MaterialController`)
- Task 4: دفعات المخزون (Batches) + `InventoryService` (شراء/صرف FIFO/إرجاع/حذف)
- Task 5: العملاء والغرف وصرف الخامات (`CustomerController`, `RoomController`, `RoomMaterialService`, `RoomService`)
- Task 6: مدفوعات العملاء (`CustomerPaymentService`, `PaymentController`)
- Task 7: المصروفات الإدارية (`ExpenseService`, `ExpenseController`)
- Task 8: حساب الربح (`ProfitService`, صفحة `/reports/profit`)
- Task 8.5: استبدال شريط التنقل العلوي بـSidebar جانبي، علامة تجارية DAWOOD
- Task 8.6/8.7: إعادة تصميم الـstyle بالكامل (لوحة ألوان دافئة "ورشة/خشب"، Sidebar غامق كعنصر توقيع، تحسين الأزرار/الجداول/البطاقات/الشارات) — **بدون أي تغيير منطقي**، باستخدام skill `frontend-design` (منقولة لـ`.claude/skills/frontend-design/`)
- إنشاء `docs/tasks-checklist.md` (checklist تفصيلي ~150 بند) و`start-agent/` (هذا المجلد)

**حالة الاختبارات:** 202/202 ناجحة (489 assertion)، `vendor/bin/pint` نظيف.

**بنود docs/tasks-checklist.md اتعلّمت:** كل بنود Tasks 0 → 8.7 (راجع الملف نفسه للتفاصيل الدقيقة).

**ملاحظات لأي Agent جاي:**
- آخر commit في git وقت كتابة هذا الإدخال: `de318f1` — يحتوي كل شغل Tasks 0–8 (backend + tests). تعديلات Task 8.6/8.7 (الـstyle) **لسه uncommitted** — تأكد من `git status` وسأل المستخدم قبل أي commit (القاعدة: **لا commit إلا بطلب صريح من المستخدم**).
- المستخدم بيتواصل بالعربي المصري العامية دايمًا — رد بنفس الأسلوب.
- المستخدم شدّد **مرارًا** على قاعدة: نفّذ → اختبر → راجع (`/code-review`) → صلّح → وثّق. متتخطاش المراجعة حتى لو الوقت ضاغط.
- المستخدم طلب صراحة عدم البدء في Task 9 (الشركاء) قبل ما يراجع شكل الواجهة الجديد بنفسه.
- لو حد سألك "تقدر تستخدم Skill X؟" وهي مش في قائمة الـSkills المتاحة ليك، قوله صراحة إنها مش متاحة واسأله يبعتهالك أو يحطها في `.claude/skills/<name>/SKILL.md` (مش أي مكان تاني في الـroot) — دي الطريقة الوحيدة اللي بتخلّيها قابلة للاستخدام عبر أداة `Skill`.
---
### 2026-08-20 — Claude (Sonnet 5، عبر Claude Code / VS Code extension) — بالتفويض لـOpenCode CLI

**النوع:** نهاية تاسك

**التاسك:** Task 9 — الشركاء والسحوبات (كامل: DB + Backend + UI + اختبارات)

**اللي اتعمل:**
- المستخدم وافق صراحة على تخطي وقفة مراجعة الواجهة اللي كانت مسجّلة في الإدخال السابق، والبدء في Task 9 مباشرة، مع تفويض كتابة الكود لأداة delegate رخيصة (OpenCode CLI، موديل `opencode/deepseek-v4-flash-free`) بدل الكتابة المباشرة. `agy` (Antigravity CLI) مش متاح على الجهاز.
- بعتّ للـOpenCode brief تفصيلي (migrations، models، PartnerService، Controllers/Requests، Views، اختبارات) مبني على قوالب Expense/Customer الموجودة فعلاً في المشروع لتقليل هامش الخطأ لموديل رخيص.
- راجعت الناتج بالكامل بنفسي: شغّلت `php artisan test --compact` و`vendor/bin/pint --test` بنفسي (مش اعتمادًا على تقرير الأداة)، قريت كل ملف اتكتب سطر سطر، اتأكدت من صيغة التقريب (round half up) ومن معادلة القسمة على 10000، ومن عدم وجود ألوان hex خارج app.css.
- لقيت ملاحظة بسيطة (مش باگ فعلي): `PartnerController::store()/update()` كانوا بيستدعوا `MoneyCast::toScaledInt()` من غير try/catch، بخلاف باقي المشروع (Expense/Payment) اللي بيلتزم بقاعدة PITFALLS.md بند 1 دايمًا. صلّحتها بنفسي مباشرة قبل الـcommit، وأعدت تشغيل الاختبارات وPint للتأكد.
- عملت commit واحد يحتوي كل ملفات Task 9 بالظبط (مفيش ملفات تانية اتلمست).
- علّمت كل بنود Task 9 في `docs/tasks-checklist.md` (✔) بعد التأكد الفعلي، وحدّثت سطر "الإجمالي" في نفس الملف.

**حالة الاختبارات:** 202/202 → 222/222 ناجحة (560 assertion، +20 اختبار جديد لـPartner). Pint نظيف. `npm run build` ناجح.

**بنود docs/tasks-checklist.md اتعلّمت:** كل بنود Task 9 (15 بند).

**ملاحظات لأي Agent جاي:**
- طريقة العمل المتبعة من دلوقتي: التفويض (delegation) لأداة CLI رخيصة (OpenCode أو agy لو اتثبّت) لكتابة الكود، والـorchestrator (أنا/اللي بعدي) بيراجع فعليًا (يشغّل الاختبارات بنفسه، يقرا الكود، يصلّح أي ملاحظة بسيطة) قبل أي commit — مش بيثق في تقرير الأداة نفسها.
- الملفات المؤقتة الخاصة بكل تفويض (brief، سجل الأحداث، النتيجة) بتتحط في مجلد الـscratchpad الخاص بالجلسة، مش في المشروع — متتفاجأش لو مش لاقيها بعد كدا، دي مش جزء من الـrepo.
- Task 10 محتاج يضيف رابط "الشركاء" في الـSidebar (`resources/views/components/app-layout.blade.php`) دلوقتي إن Task 9 خلص — دا كان مؤجل عمدًا لحد هنا.
- Task 11 (اختبار الدورة الكاملة) لازم يستخدم الجدول المرجعي بالظبط من `docs/tasks.md` (خزنة نهائية 14,500، ربح 27,460) — الأرقام دي لازم تتحقق حرفيًا، مش تقريبيًا.
---
### 2026-08-20 — Claude (Sonnet 5، عبر Claude Code / VS Code extension) — بالتفويض لـOpenCode CLI

**النوع:** نهاية تاسك

**التاسك:** Task 10 — تجانس الواجهة والتنقل (رابط الشركاء + مكونات Blade مشتركة + تدقيق Design Tokens)

**اللي اتعمل:**
- قبل ما أبعت أي تفويض، عملت التدقيق المطلوب بنفسي (`grep` على كل `resources/views` بحثًا عن hex/font-family خارج app.css، وعن مبالغ متعرضة من غير `<x-money>`، وعن تنسيقات تاريخ مختلفة) — النتيجة **نظيفة 100%** من الأساس، فوفّرت وقت التفويض في إعادة اكتشاف حاجة مؤكدة أصلاً.
- ضفت رابط "الشركاء" للـSidebar (`app-layout.blade.php`) **بنفسي مباشرة** من غير تفويض — تعديل سطر واحد، صفر مخاطرة.
- بعتّ لـOpenCode (نفس الموديل الرخيص `opencode/deepseek-v4-flash-free`) تاسك استخراج 3 Blade components مشتركة (`<x-data-table>`, `<x-field>`, `<x-delete-button>`) وتحويل ~23 ملف view ليستخدموها، بشرط "صفر تغيير شكلي" (نفس الـclasses بالظبط).
- راجعت الناتج بالكامل: شغّلت الاختبارات وPint بنفسي (222/222 زي ما كانت)، أعدت تشغيل تدقيقي الأصلي على الملفات المتغيرة، وقريت الـdiff كامل لعينة تمثيلية من كل نمط (جدول بسيط، جدول جوه صفحة فيها نماذج تانية، _fields partial، create.blade.php مباشر) — كل حاجة طابقت المطلوب حرفيًا، من غير أي "تسريب نطاق" (scope creep) أو ملفات زيادة.
- عملت commit واحد لكل ملفات Task 10 بالظبط.
- علّمت كل بنود Task 10 في `docs/tasks-checklist.md` وحدّثت سطر "الإجمالي".

**حالة الاختبارات:** فضلت 222/222 ناجحة طول الوقت (الـrefactor مالوش أي تأثير منطقي). Pint نظيف، `npm run build` ناجح.

**بنود docs/tasks-checklist.md اتعلّمت:** كل بنود Task 10 (10 بنود).

**ملاحظات لأي Agent جاي:**
- **Task 11 هو الباقي الوحيد** — اختبار الدورة الكاملة (Integration Test) بالأرقام المرجعية بالظبط من `docs/tasks.md` (خزنة نهائية 14,500، ربح 27,460، نصيب شريك 20% = 5,492 بعد سحب 2,000).
- لو محتاج تضيف جدول أو نموذج جديد في الواجهة مستقبلًا، استخدم `<x-data-table>`, `<x-field>`, `<x-delete-button>` الموجودين في `resources/views/components/` بدل ما تكرر الـmarkup يدويًا — دول بقوا الطريقة القياسية في المشروع.
- `<x-field>` مخصص بس للـinput العادي (text/number/date) — الـ`<select>` والحقول اللي تحتها نص توضيحي (زي نسبة الشريك) اتسابت عمدًا markup عادي، متحاولش تجبرها جوه الـcomponent.
---
