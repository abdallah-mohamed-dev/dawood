# دليل استخدام النظام

شرح مبسّط لكل جزء في النظام، لأي مستخدم بيشتغل عليه يوميًا.

> **للـAgents:** الملف دا **إلزامي التحديث** مع أي تاسك بيغيّر منطق الأعمال (قاعدة حسابية، حالة جديدة، قاعدة تحقق، سلوك حذف/تعديل، شاشة جديدة). التفاصيل في `CLAUDE.md` القسم 5. الكتابة هنا **بلغة المستخدم العادي** — مفيش مصطلحات تقنية ولا أسماء كلاسات.

---

## 1. تسجيل الدخول

بتدخل بالإيميل والباسورد اللي معاك (المدير هو المستخدم الوحيد حاليًا). بعد الدخول بتوصل لصفحة **Dashboard**.

### الملف الشخصي

من الشريط الجانبي، هتلاقي **اسمك** مكتوب تحت خالص فوق زرار "تسجيل الخروج" — اضغط عليه تدخل صفحة **الملف الشخصي**، وتقدر منها:

- **تغيّر اسمك وإيميلك** — الإيميل ده هو اللي هتدخل بيه المرة الجاية، فخلي بالك.
- **تغيّر كلمة المرور** — لازم تكتب كلمة المرور **الحالية** الأول (حماية لو حد لقى الجهاز مفتوح)، وبعدين الجديدة مرتين. الجديدة لازم تكون **8 حروف على الأقل**.

بعد تغيير كلمة المرور بتفضل داخل عادي، مش هتتطلع برة.

## 2. المواد (المخزون)

- كل **مادة** ليها اسم ووحدة قياس (قطعة، متر، لوح...).
- **اسم المادة لازم يكون فريد** — مينفعش مادتين بنفس الاسم في النظام.
- بيظهر لكل مادة **الكمية المتاحة حاليًا** في المخزون.
- مينفعش تمسح مادة ليها مشتريات أو مستخدمة في غرفة.

## 3. شراء المخزون (Purchases)

كل عملية شراء بتتسجل كـ"دفعة" مستقلة (سعر شراء + كمية + تاريخ)، حتى لو نفس المادة اتشرت قبل كده. ده مهم عشان لما تصرف من المخزون، النظام يحسب التكلفة الحقيقية بدقة (بيصرف من أقدم دفعة الأول).

- شراء بيقلل رصيد الخزنة فورًا.
- مينفعش تمسح دفعة اتصرف منها حاجة.

## 4. العملاء

بيانات بسيطة: اسم، تليفون، عنوان. **مينفعش تمسح عميل ليه غرف مسجّلة** — لازم تتصرف في الغرف الأول.

## 5. الغرف (طلبات العملاء)

كل غرفة (طلب) بتتبع عميل معيّن، وليها **سعر بيع** و**حالة**:

| الحالة | المعنى |
|---|---|
| مسودة (draft) | لسه ما بدأش التنفيذ |
| قيد التنفيذ (in_progress) | شغالين عليها |
| مكتملة (completed) | اتسلمت للعميل |
| ملغاة (cancelled) | اتلغت |

### صرف المواد للغرفة
بتحدد للغرفة المواد اللي محتاجاها (الاحتياج)، وبعدين تصرفها فعليًا خطوة منفصلة. النظام **يرفض** أي صرف أكبر من المتاح في المخزون، أو أكبر من الاحتياج المحدد.

### حذف غرفة اتصرفلها مواد
النظام بيسألك: تحب **ترجّع** المواد المصروفة للمخزون، ولا تعتبرها **استهلكت فعلًا**؟ القرار ده مهم لأنه بيأثر على المخزون المتاح والتكلفة المحسوبة.

⚠️ **مهم:** تحويل الغرفة لـ"ملغاة" **مش بيرجّع المواد تلقائيًا** — لو عايز ترجّعها فعلًا، لازم تمسح الغرفة نفسها.

## 6. دفعات العملاء

كل ما العميل يدفع فلوس على غرفة، بتسجّلها هنا. النظام بيحسب تلقائيًا:

- **المدفوع** = مجموع كل الدفعات على الغرفة.
- **المتبقي** = سعر البيع − المدفوع.

مينفعش تسجّل دفعة تخلي المدفوع يزيد عن سعر البيع — النظام هيرفضها ويقولك المتبقي الفعلي كام.

كل دفعة بتزود رصيد الخزنة فورًا.

## 7. المصروفات الإدارية

مصروفات التشغيل العامة (كهرباء، إيجار...إلخ) — مقسّمة لبنود (Categories) زي "كهرباء" و"إيجار". كل مصروف بتسجله بينقص من رصيد الخزنة فورًا، ويتحسب بالكامل على الربح فورًا (بعكس تكلفة المواد اللي بتتحسب بس لما الغرفة تكتمل — راجع قسم الأرباح تحت).

## 8. الخزنة (Cashbox)

صفحة الخزنة بتوريك **رصيد فلوس الشركة الفعلي** لحظة بلحظة — كل جنيه دخل أو خرج فعليًا:

- **دخول:** دفعات العملاء، الرصيد الافتتاحي.
- **خروج:** شراء مخزون، مصروفات، سحوبات الشركاء.

الرصيد الافتتاحي بيتحدد مرة واحدة (وممكن تعدّله بعدين، مش بتتسجل مرة تانية).

**صفحة الخزنة للعرض فقط** — مفيش تسجيل حركة يدوي منها، كل حركة بتيجي تلقائي من العملية الأصلية (دفعة، مصروف، شراء، سحب).

---

## 9. الأرباح — الجزء الأهم 💰

### الفرق الأساسي: رصيد الخزنة ≠ صافي الربح

النظام بيفرّق بين رقمين مختلفين تمامًا، ومحدش يتلخبط بينهم:

| | رصيد الخزنة | صافي الربح |
|---|---|---|
| **معناه** | فلوس فعلية موجودة دلوقتي | أداء الشركة الحقيقي |
| **بيشمل ايه** | كل حركة فلوس حصلت فعلًا (حتى لغرفة لسه مش مكتملة) | بس الغرف **المكتملة** + كل المصروفات الإدارية |

**مثال:** ممكن تاخد دفعة 10,000 ج.م من عميل على غرفة لسه "قيد التنفيذ" — الرصيد بتاعك في الخزنة بيزيد فورًا بـ10,000، لكن الربح **مش بيتأثر خالص** لحد ما الغرفة تتسجل "مكتملة".

### إزاي بيتحسب صافي الربح؟

```
صافي الربح = (سعر بيع كل الغرف "المكتملة")
            − (تكلفة المواد اللي اتصرفت على الغرف "المكتملة" بس)
            − (كل المصروفات الإدارية)
```

- لو غرفة لسه "قيد التنفيذ" وصُرفلها مواد، تكلفة المواد دي بتتحسب كـ**"شغل تحت التنفيذ"** (مش خسارة ولا ربح) — بتتحول لتكلفة فعلية بس لحظة ما الغرفة تخلص وتبقى "مكتملة".
- المصروفات الإدارية بالعكس: بتتحسب **فورًا** لحظة تسجيلها، بغض النظر عن حالة أي غرفة.

**مثال كامل:**

```
غرفة سعرها 30,000 ج.م، لسه "قيد التنفيذ"، اتصرفلها مواد بـ 540 ج.م، وفيه مصروف 2,000 ج.م
→ الربح دلوقتي = 0 - 0 - 2,000 = -2,000 (خسارة مؤقتة، لسه معلقة)

لما الغرفة تتسجل "مكتملة":
→ الربح = 30,000 - 540 - 2,000 = 27,460 ج.م
```

## 10. الشركاء وسحوباتهم

كل شريك ليه **نسبة مئوية** من الأرباح (مجموع نسب كل الشركاء لازم ≤ 100%).

### نصيب الشريك
```
نصيب الشريك = صافي الربح × نسبته٪
```
النصيب ده **بيتحدث أوتوماتيك** أول ما الربح يتغيّر (غرفة جديدة اتكملت، مصروف جديد...) — مش رقم ثابت مخزّن.

### السحب
الشريك ممكن "يسحب" من نصيبه. النظام بيوريك:
- **الإجمالي المسحوب** = مجموع كل سحوباته.
- **المتبقي له** = نصيبه − اللي سحبه.

⚠️ لو الشريك سحب أكتر من نصيبه، أو الربح بقى صفر/سالب بعد ما سحب، **المتبقي هيبقى بالسالب** — النظام هيعرضهولك بوضوح (تنبيه أحمر) لكن مش هيمنعك من السحب، القرار ليك كأدمن.

كل سحب بينقص من رصيد الخزنة فورًا زي أي مصروف.

## 11. تقرير الأرباح

صفحة `/reports/profit` بتجمّعلك:
- صافي الربح الحالي.
- قيمة الشغل تحت التنفيذ (الغرف اللي لسه مش مكتملة وصُرفلها مواد).
- قيمة المخزون المتبقي (اللي اتشري ولسه ما اتصرفش).
- نصيب كل شريك ومتبقيه.

الهدف من التقرير: تشوف الصورة المالية الكاملة للشركة في نظرة واحدة، وتفرّق بوضوح بين "فلوس في الخزنة" و"ربح حقيقي مُستحق".

---

*لأي تفاصيل تقنية أعمق (حسابات دقيقة، حالات خاصة)، راجع ملفات `docs/` في المشروع.*
